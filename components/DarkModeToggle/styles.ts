import { TerminalFooterLinkIcon } from '@styles/terminal/terminalFooter';
import { styled } from '@styles/theme';

export const DarkModeToggleWrapper = styled.div`
  /* align-self: flex-end; */
`;

export const DarkModeToggleButton = styled.button`
  all: unset;
`;

export const DarkModeToggleIcon = styled(TerminalFooterLinkIcon)`
  padding: 0 15px;
`;
